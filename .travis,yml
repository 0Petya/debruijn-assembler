before_install:
    npm install -g browserify
    npm install -g uglify-js

language: elm
elm:
    - elm0.19.0

script:
    - make env=prod

deploy:
    provider: pages
    skip_cleanup: true
    github_token: $GITHUB_TOKEN
    on:
        branch: master
